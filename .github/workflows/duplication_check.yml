name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: recipe duplication check
        run: |
          ruby .github/scripts/duplication_check.rb
